# Mutation Testing

Run: npx stryker run

This uses Stryker Mutator to verify test quality by introducing
mutations (code changes) and checking if tests catch them.

Install: npm install --save-dev @stryker-mutator/core @stryker-mutator/vitest-runner
