"""Backend repository wrappers — re-export desktop repositories.

Import from here instead of the root ``repositories/`` package so the
backend can eventually diverge to its own data access implementations.
"""
