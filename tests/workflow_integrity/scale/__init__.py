"""Scale-validation tests — blueprint Appendix A Phase 6.2 (future roadmap).

Gated behind the ``scale`` marker; the module never runs in the default suite.
The companion calibration (6.1) / certification (6.3) documentation lives in
``tests/workflow_integrity/reports/README.md``.
"""