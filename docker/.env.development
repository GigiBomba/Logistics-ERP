# ── Development Environment ────────────────────────────────────────
OPERION_DB_ENGINE=sqlite
OPERION_DB_PATH=data/cashflow.db
OPERION_API_BASE_URL=http://localhost:8000

# Rate limiting (in-memory, less strict in dev)
OPERION_RATE_LIMIT=1000
