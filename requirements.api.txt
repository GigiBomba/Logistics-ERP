# Server-side dependencies for the FastAPI backend only.
# Excludes Windows-only and desktop GUI packages (PySide6, pywin32, etc.)
# Lockfile: requirements.lock.txt (regenerate with pip-compile requirements.api.txt)

# --- Web / HTTP ---
requests>=2.32.5
httpx>=0.28.1
httpcore>=1.0.9
urllib3>=2.6.3
h11>=0.16.0
certifi>=2026.1.4
charset-normalizer>=3.4.4

# --- FastAPI / ASGI server ---
fastapi>=0.115.0,<0.200.0
starlette>=0.40.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# --- Data processing ---
numpy>=1.26.4
python-dateutil>=2.9.0.post0
pytz>=2026.2

# --- Pydantic ---
pydantic>=2.13.4
pydantic-settings>=2.3.0

# --- Auth / Security ---
bcrypt>=4.2.0
PyJWT[crypto]>=2.13.0
cryptography>=50.0.0,<51.0.0

# --- Database ---
redis>=5.0.0
psycopg2-binary>=2.9.0
alembic>=1.13

# --- PDF / Documents ---
pypdf>=6.13.0
pikepdf>=9.11.0
reportlab>=4.4.10

# --- Excel ---
openpyxl>=3.1.5

# --- Scheduling ---
celery>=5.3.0
click>=8.1.8
colorama>=0.4.6
colorlog>=6.10.1
tenacity>=9.1.2

# --- QR / Barcode ---
qrcode>=8.0.0

# --- Translation ---
deep-translator>=1.2.0
Babel>=2.18.0

# --- AI / Copilot ---
google-genai>=1.47.0

# --- Firebase / Push notifications ---
# FCM/APNs delivery for alert/approval pushes (services/operations/push_sender.py).
# firebase-admin handles both transports from one token namespace.
firebase-admin>=6.5.0

# --- Monitoring ---
prometheus-client>=0.20.0
# Error tracking — no-op when OPERION_SENTRY_DSN is unset (backend/main.py).
sentry-sdk>=2.0.0

# --- System ---
psutil>=6.0.0
